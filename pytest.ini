[pytest]
# Repo root and scripts/ on sys.path so tests can import the top-level
# pipeline modules and scripts/download_noaa.py directly, without needing
# __init__.py files or package installation.
pythonpath = . scripts
testpaths = tests
