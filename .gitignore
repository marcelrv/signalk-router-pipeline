# Internal background material — not for public distribution
local_only/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Raw chart downloads / working data (large, re-downloadable, not committed)
# Blanket-ignore data/ so pilot clip output (clip_pilot_data.py) is never committed.
data/
data/raw/
data/cache/
*.sqlite
*.sqlite.gz
!test/fixtures/*.sqlite

# OS / editor
.DS_Store
.vscode/

# Local Claude Code settings (machine-specific, not for public distribution)
.claude/settings.local.json
